import './Features.css';

function Features({ className }) {
  return (
    <div className={'features ' + className}>
      <h2>Features</h2>
      <p>Inserts stuff...</p>
    </div>
  );
}

export default Features;
